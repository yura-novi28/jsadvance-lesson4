import { SearchFirstNamePipe } from './search-first-name.pipe';

describe('SearchFirstNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFirstNamePipe();
    expect(pipe).toBeTruthy();
  });
});
